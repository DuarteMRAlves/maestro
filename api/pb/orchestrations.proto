syntax = "proto3";

option go_package = "github.com/DuarteMRAlves/maestro/api/pb";

package pb;

import "google/protobuf/empty.proto";

service OrchestrationManagement {
  rpc Create(Orchestration) returns (google.protobuf.Empty);
  rpc Get(Orchestration) returns (stream Orchestration);
}

message Orchestration {
  string name = 1;
  string phase = 2;
  repeated string links = 3;
}

